services:

  backend:
    build: ./backend
    ports:
      - "3001:3001"

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend

  cache:
    image: redis:7-alpine
    container_name: cache